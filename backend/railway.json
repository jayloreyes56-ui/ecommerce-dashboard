{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --working-dir=backend --no-dev --optimize-autoloader && npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "cd backend && php artisan migrate --force --seed && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
